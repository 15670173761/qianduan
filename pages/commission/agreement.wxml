<view class="panel">
    <view class="content">
        <view wx:for="{{contents}}">{{item}}</view>
        <view wx:if="{{contents.length <= 0}}" style="text-align: center; text-indent: 0; margin-top: 100rpx; color: #ccc;">暂无内容</view>
    </view>
</view>